version: 2.1
jobs:
  build:
    docker:
      - image: circleci/node:11.10.1
    steps:
      - checkout
      - run: apt update

  lint:
    docker:
      - image: circleci/node:11.10.1
    steps:
      - checkout
      - run: apt update

  test:
    docker:
      - image: circleci/node:11.10.1
    steps:
      - checkout
      - run: apt update

workflows:
  version: 2
  integration:
    jobs:
      - build
      - lint
      - test
